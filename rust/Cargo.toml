[package]
name = "monte_carlo_european_options"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
rand_distr = "0.4"
csv = "1"
serde = { version = "1", features = ["derive"] }
num_cpus = "1"

